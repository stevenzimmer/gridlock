import { Game } from "@/components/Game";

export default function Page() {
  return (
    <main className="mx-auto flex min-h-screen w-full max-w-6xl flex-col px-3 py-4 sm:px-6 sm:py-8">
      <Game />
    </main>
  );
}
